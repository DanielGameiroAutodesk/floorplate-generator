.UnitInput {
  position: relative;
  width: 100%;

  & input {
    width: 100%;
  }
}

.Unit {
  position: absolute;
  right: 0.4em;
  line-height: 24px;
  color: var(--text-color-light);
  pointer-events: none;
}
