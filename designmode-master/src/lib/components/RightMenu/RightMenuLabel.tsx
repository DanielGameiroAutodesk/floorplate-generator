import styles from "./RightMenuLabel.module.pcss"

// This is a class name export instead of a component due to
// how labels were used at the time when the code was refactored,
// to limit scope of the refactor.
export const labelClassName = styles.Label
